#include "hash_table.hpp"



   /* 
    std::function<int(const T&)> hash_function;
    std::size_t buckets_num;
    std::list<T> buckets;
    std::size_t collision_count;
    public:
    hash_table () = delete;
    hash_table (std::size_t buckets, std::function<int(const T&) hashing=std::hash<T>);	

    void add(const T& element);
    const T& find(std::size_t hash);
};

*/
